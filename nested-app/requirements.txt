# Nested App - Tests recursive directory scanning
# QuickChain should find this file even though it is not in the root directory

# Known Vulnerable - Flask with known CVEs
flask==3.1.3

# Known Good
numpy==1.24.0
